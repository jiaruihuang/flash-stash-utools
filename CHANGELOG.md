# 更新日志

版本规则：feature 按语义化版本管理，plugin.json 中的 version 为唯一权威源，package.json 同步维护（npm run pack 会校验两者一致）。

## 0.5.5 (2026-09-11) — bugfix

### 修复
- **代码块「复制」按钮点不中（"有点飘"）**：浮层按钮是代码块的**兄弟节点**（不侵入 ProseMirror 内容 DOM），指针从代码块移向按钮时会先触发代码块的 `mouseout`，原先立即收起 → 按钮**在指针到达前就消失了**，于是"点不中"。现加**鼠标桥**：收起改为 160ms 延迟，指针落在按钮上、或回到代码块都会取消收起；按钮同时稍作放大（padding 3→5px、圆角 8px）更好点
- 回归测试锁定真实指针路径：`mouseover 代码块 → mouseout(relatedTarget=按钮) → 点击`，并验证指针离开后仍会延时收起

### 其他
- **用户 Windows 实测通过（0.5.5）**：原 P0「Markdown 富文本编辑器体验」正式关闭——多行/H1/列表/引用各自生效、代码块高亮与语言角标、语言选择器、表格行列与矩阵选择器、选多行合并代码块、列表逐行成项、表格末尾光标、代码块复制按钮全部符合预期
- 项目当前**没有未完成待办**（见 HANDOFF §6）
- **发布准备**：`plugin.json` 的 `author` 由占位值改为实际开发者名称 **doubleXiao**（需与 uTools 开发者账号一致）

## 0.5.4 (2026-09-11) — bugfix

### 修复
- **选多行点「代码块」会生成多个代码块**：代码块按钮改为智能命令 `toggleCodeBlockSmart()`——选区跨多个块时把内容**合并进一个代码块**（源码里只有一对围栏，行与行之间是普通换行）；光标在单个块内时仍是常规切换
- **表格末行按 ↓ 出现"横向光标"**：那不是新增了一行，而是 ProseMirror 的 **gap cursor**（默认 CSS 渲染成 20px 宽的横线）。现启用 TrailingNode（仅当文末不是段落时——如以表格/代码块/引用结尾——自动补一个空段落），↓ 能落到**真实文字光标**并能直接继续输入；同时把 gap cursor 样式改成竖向光标作兜底
- **源码尾部多出空行**：`htmlToMarkdown` 现在去掉文末空白，TrailingNode 补的空段落不再留痕；也保证「打开编辑不改动 → 不判脏」（新增回归测试锁定）

### 新增
- **列表命令逐行成项**：粘贴/软换行来的多行文字（同段落内的 `<br>`）点「• 列表 / 1. 列表」时，先按行拆块再套列表——**每行各成一项**，不再是一个列表项里塞多行

### 关于「每行一个空行」的专业结论（不再反复改动，见 HANDOFF §7）
- **回车 = 新段落**：多行即多块，这是 H1/列表/引用能**按行生效**的前提；Markdown 段落之间**本就以空行分隔**（Typora / Vditor / Milkdown 一致）
- 想让多行留在**同一段**（源码里相邻行、块级格式整体生效）→ 用 **Shift+Enter** 软换行
- 「选多行 → 代码块」一律合并为**一个**代码块；「选多行 → 列表」逐行成项——两个高频动作都不再"碎裂"

### 测试
- 新增/改写 6 例：多段合并成一个代码块、单块常规切换、表格结尾自动补段落且 ↓ 非 gap cursor、软换行多行逐行成项、以代码块结尾的收藏打开不判脏（当前 97 例全绿）

## 0.5.3 (2026-09-11) — bugfix

### 修复（0.5.2 实测反馈）
- **块级格式作用于整段**（H1/列表/引用）：根因是 0.5.2 按字面要求把回车改成软换行（`<br>`），多行文本变成**同一个段落**，于是块级命令只能整段生效（选一行设 H1、选多行设列表都会"糊成一块"）。现**回到行业标准**：**回车 = 新段落**、**Shift+Enter = 段内软换行**（与 Typora/Vditor/Milkdown 一致），多行即多段，块级格式天然按行生效
- **引用/列表部分选中会扩到整段**：新增 `prepareBlockSelection()`——块级命令执行前**按选区边界把段落切开**（终点后切、起点前切，并显式重设选区），引用 / 列表 / 标题现在**只作用于选中的那段文字**，其余保持原样
- **行内代码在段末继续输入仍留在代码样式里**：Tiptap 的 `code` mark 默认 `inclusive`，光标停在代码末尾时后续输入会被"吸"进代码。现用 `Code.extend({ inclusive:false })` 覆盖（StarterKit 的 `code` 关闭），代码末尾回车/继续输入都是普通文本

### 新增
- **代码块语言角标**：编辑区与预览区的代码框左上角显示当前语言（显式设置的语言，未设置时显示 highlight.js **自动识别**结果，带缓存）；纯装饰，不写入文档

### 协作原则（用户要求，已写入 AGENTS.md 长期有效）
- 用户反馈表达的是**意图**而非精确规格：实施时按专业工程师视角还原真实问题，必要时纠正字面要求并说明理由与代价（0.5.2 的软换行就是"照字面做"的反面教材，见 HANDOFF §7 第 13/14 条）

### 测试
- 新增/改写 8 例：回车=新段落与 Shift+Enter 软换行、部分选中切块（引用/列表/标题）、光标所在行才受影响、行内代码末尾输入、编辑区与预览区语言角标（当前 91 例全绿）

## 0.5.2 (2026-09-11) — bugfix

### 修复（0.5.1 实测反馈，含两个数据丢失级 bug）
- **表格「富文本→源码→富文本」后表格丢失、内容变乱码**：根因有两层——① turndown 对**空白节点**在规则匹配前短路（`blankRule`），全空表格会被整表丢弃；② 单元格里的块级 `<p>`（尤其空 `<p></p>`）会带出 `\n\n`，把 GFM 表格行结构打断，再渲染就成了 `<p>| |</p>` 之类的乱码。现改为**自研 table 规则**：直接遍历 rows/cells、逐格递归转换 + 空白归一 + 转义 `|` + 自动补表头分隔行；并在转换前给 table 插入零宽空格占位（U+200B 不参与 trim）绕开空白短路
- **代码块切到预览/源码再回富文本后内容消失、多行变单行**：根因是 hast 序列化把 lowlight 的**根节点**也当成元素输出，产出 `<undefined>…</undefined>` 包裹层——有 token 时被 DOMPurify 剥壳后侥幸留下内容，**未识别出语言时（空树）连内容一起被剥光**。现修正序列化器（根节点只输出子内容），并对「未识别语言返回空树」补了回退：原样输出转义纯文本
- **换行后源码里多出空行**：富文本里回车原本等于「新段落」（Markdown 里就是空行）。现改为**回车 = 软换行**（`<br>` → 源码相邻两行），**Shift+Enter = 新段落**；列表内（续编）与引用内（另起引用段落 + 空段退出）仍走引擎行为；同时收紧段落间距
- **代码块退出方式**：去掉当年「空行回车退出」的兜底，改用引擎标准行为——**末行按 ↓ 退出**、三连回车退出、块内回车换行
- **删除代码块后悬停「复制」按钮不消失**：浮层现在会随编辑器更新检查代码块是否已被删除/移出并立即收起，鼠标移出代码块也会收起

### 新增
- **代码语言选择器**（工具栏）：光标在代码块内时可从语言列表（highlight.js common 语言集）选择语言，自动重新高亮并写入源码围栏（` ```java `）；不在代码块内时置灰
- **表格行列矩阵选择器**：点「⊞ 表格」弹出 8×8 矩阵，悬停预览 `N × M` 并高亮，点击插入对应行列（替代原固定 3×3）

### 测试
- 新增/改写 12 例：空表格与无语言代码块往返完整性、回车软换行与 Shift+Enter 段落、代码块 ↓ 退出、语言选择器、表格矩阵选择器、复制浮层删除后收起、未识别语言不丢内容、语言属性写回围栏（当前 83 例全绿）

### 已知限制
- 富文本 ↔ 源码/预览切换会重建编辑器，**撤销历史随之重置**（Markdown 源码是唯一数据源）；同一次富文本会话内撤销/重做正常

## 0.5.1 (2026-09-11) — feature

### 修复（用户 Windows 实测反馈）
- **富文本输入时内容外出现黄色框**：contenteditable 的浏览器默认 focus 环所致，已 CSS 强制 `outline:none`（`.rich .ProseMirror` 含 :focus/:focus-visible）
- **表格插入后看不见**：`.rich` 缺少 table/th/td 样式（此前只有预览区有），表格实际存在但无边框不显示；已补齐边框、表头底色与选中格高亮
- **点击链接把插件跳成空白页**：webview 内 `<a>` 默认导航离开插件；新增全局委托拦截，`<a>` 一律走 `utools.shellOpenExternal` 用系统浏览器打开（编辑器/预览/弹窗统一生效）

### 新增
- **代码块升级**：lowlight/highlight.js 语法高亮——编辑区内由 CodeBlockLowlight 装饰渲染、预览/弹窗由 marked 渲染器输出，共用同一套 hljs 主题色且浅深色自适应；围栏带语言自动按语言高亮，无语言自动识别；代码框改为深色卡片样式；**编辑区与预览区均支持悬停「复制」按钮**（不侵入 ProseMirror 内容 DOM，复制纯文本）
- **链接按钮改为「插入链接」对话框**：有选区→包裹为链接；无选区→输入「链接文字+网址」插入命名链接（网址校验，回车可确认）
- **表格工具栏补行列增删**：行+ / 行- / 列+ / 列-（addRowAfter / deleteRow / addColumnAfter / deleteColumn）

### 调研
- uTools 插件「備忘速记」（作者 smiling，闭源）：详情页无技术信息，`.upxs` 加密容器无法解包验证；按其功能特征（所见即所得/即时渲染、`/` 斜杠菜单、右键表格插入合并）推断最接近 **Vditor 即时渲染（IR）模式** 或 **Milkdown Crepe** 类实现，无开源源码可确认

### 测试
- 新增 6 例：链接对话框命名插入、预览弹窗链接走系统浏览器、编辑区代码块复制浮层、表格行列增删、编辑区与预览高亮 token（当前 74 例全绿）
- 体积：dist JS 约 706KB（gzip 225KB，随语法高亮库增长）

## 0.5.0 (2026-09-11) — feature

### Markdown 富文本编辑器换引擎（P0 待办关闭）
- **告别 contenteditable + document.execCommand**：富文本编辑全部迁移到 **Tiptap 3（ProseMirror）**，新增 `src/editor.ts` 统一封装。列表续编/嵌套、粘贴格式清洗、IME 组合输入、表格、代码块等此前反复修补的痛点由引擎原生解决
- **编辑器三模式保留**：富文本 / 源码 / 预览 tab 不变；保存、未保存判定（baseline 规范化管线）、Esc 行为全部保持既有逻辑
- **工具栏重做（命令化）**：加粗 / 斜体 / 下划线（新）/ 删除线 / H1-H3 / 无序·有序列表 / 引用 / 代码块 / 行内码 / 链接（真链接标记）/ **表格插入·删除（新）** / **撤销·重做（新）**，全部走 `editor.chain()` 命令而非 execCommand
- **回车语义延续旧手感**：自定义 LegacyEnter 扩展保住了「代码块内空行回车退出块」「引用内空段回车退出引用」；标题回车、列表续编由引擎接管
- **表格 Markdown 往返修复**：切换 gfm 规则（turndown-plugin-gfm）支持表格/任务列表/删除线回写；单元格内 `<p>` 压行内、转换前剥离 Tiptap 的 `<colgroup>`（否则 gfm 表格规则误判整表保留）
- **源码/预览复用同一渲染管线**（marked + DOMPurify → 富文本；turndown → 源码），无新增往返漂移

### 测试
- 新增 `tests/editor.test.ts`（13 例命令级测试）：工具栏各命令、撤销/重做、表格增删、Markdown↔HTML 往返（含表格 gfm 保留）、LegacyEnter 键盘级回车语义
- ui-smoke 富文本用例重写为命令/键盘驱动（jsdom 补 rAF/getComputedStyle/Range 测量 API 兜底；编辑器实例经 `.rich` 上的 `_mdEditor` 测试接缝获取）
- 当前 **68 例全绿**
- 体积：dist JS 约 537KB（gzip 172KB，此前 117KB/39KB），本地加载无感；零 UI 框架原则保留

## 0.4.12 (2026-09-11) — feature

### 新增
- **设置项「⚡ 复制后立即关闭窗口」**（主面板「⚙ 设置」可开关，默认关闭）：开启后所有复制操作——主面板点卡片 / 回车复制 / 「复制」按钮、预览弹窗内「复制内容」——在复制成功的一瞬间直接退出 uTools 并恢复之前窗口，**不弹任何提示气泡、零等待**，即刻回到原工作继续干活；关闭时保持原行为（toast 提示）。复制失败不关闭窗口，保留错误提示
- 所有复制路径集中在同一个 `doCopy()`，设置即时生效，无需重启

### 测试
- 新增 4 例：设置开关持久化与恢复默认、开启时主面板复制即刻退出且无 toast、关闭时保持现状、预览内复制同样即刻退出；设置读写单测补 `closeOnCopy`（当前 55 例全绿）

## 0.4.11 (2026-09-11) — feature

### 新增
- **主面板卡片「预览」按钮**：点击卡片仍是复制，但每条收藏新增独立「预览」按钮——打开弹窗展示完整内容：Markdown 渲染为富文本（带代码块/表格样式）、纯文本保持换行原样、图片显示大图，另附备注、标签与更新时间；弹窗内可直接「复制内容」，Esc / 点遮罩 / ✕ 关闭均不退出插件（不触发复制）

### 修复
- **标签建议列表被 Esc 关闭后点击不弹出**：Esc 收起列表时输入框仍保持焦点，之后鼠标再点输入框不再触发 focus，列表"点不开"。现支持单击/双击输入框重新弹出；同时修复失焦 150ms 后延迟收起的竞态——收起定时器还没到就被点回时，列表会先弹出又被迟到的回调收掉，现点击时先取消待执行的收起

### 测试
- 新增 3 例回归：Esc 后单击/双击重开列表、失焦延迟收起竞态、预览弹窗完整内容与复制（当前 51 例全绿）

### 其他
- `npm run pack` 现在会**自动清理旧版本发布物**：只保留当前版本的 `release/flash-stash-<ver>/` 目录与根目录 `flash-stash-<ver>.zip`，旧目录/旧 zip 一并删除（清理失败不阻塞打包）

## 0.4.10 (2026-09-11) — bugfix

### 修复
- **toast 成功提示的迟到退出定时器**：toast 风格保存后有一个 1400ms 的 `finishAndExit` 定时器，此前不可取消——用户在 toast 展示期间按 Esc / 提前离开后再做别的事，定时器晚到会重绘视图（把正在输入的新增视图劫持回主面板、内容丢失）。现改为可跟踪的 `toastTimer`，提前离开（commitLeave）时一并清除；新增回归测试锁定
- **Agent 文档体系**：内容**单点存放**于根 `AGENTS.md`（agents.md 开放标准，OpenCode 直接读取，含必读引用表）；`CLAUDE.md`（Claude Code）与 `.agents/agent.md`（DSH）为**薄引用入口**——只指回根 AGENTS.md、不再复制内容（同一份信息，三个工具入口互相引用）

### 其他
- 用户 Windows 实测通过（0.4.10）：未保存提示各场景、标签键盘双焦点、logo 128 打包提示均符合预期
- 工程/测试环境就绪，原 P2 工程待办全部关闭

## 0.4.9 (2026-09-11) — feature

### 未保存提示与离开去向（新增/修改时任何改动都会提示）
- **保存视图区分入口来源（`SaveInit.from`）**：「主面板＋新增」进入的保存视图（from: main），Esc / 取消 / 保存成功 / 成功浮层关闭后一律**回主面板**，不再退出插件（修复「＋新增后 Esc 直接退回 uTools」）；由 uTools 功能指令（收藏文本/Markdown/图片/剪贴板）进入的新建（from: utools）仍保持「离开 = 退出插件」
- **有未保存修改时弹确认**：内容（含类型切换）/ 标签（含标签框里已输入但未回车的文字）/ 备注任一改动，Esc 与「取消」按钮都会弹「未保存的修改」确认框（放弃离开 / 取消留在原地）；只打开未改动则不弹（修复历史版本「任何地方打开退出都误弹」的回归风险，基线判定沿用 0.4.8 规范化管线）
- **成功浮层「继续收藏」不再 location.reload（会白屏）**：改为回调 `cb.onNewAgain()` 新开一条空收藏视图（保持同来源）；手动点「继续收藏 / 去查看」时会取消自动退出的定时器，避免旧定时器在切换后误触发

### 修复
- 确认框防抖：连按 Esc / 连点「取消」不再叠加多个确认框

## 0.4.8 (2026-09-11) — bugfix

### 修复
- **Escape 交互重构**：上一版全局 capture 拦截器把事件掐死在 document 阶段，导致弹窗、标签建议列表的 Escape 全部失效（"esc 没有任何反应"）。改为集中式处理器栈（src/escape.ts，LIFO，最近打开的层优先响应）：
  - 标签建议列表弹出时：Esc 关闭列表回输入框，不离开保存视图
  - 弹窗（设置/标签管理/更多标签/确认框）：Esc 关闭当前弹窗，再按 Esc 才作用于下一层
  - 标签管理·重命名输入中：Esc 只取消重命名，弹窗保持打开
  - 保存视图：有未保存修改 → 弹确认框；无修改 → 直接离开（编辑→主面板，新建→退出插件）
  - 主面板：Esc 清空搜索词，再按 Esc 退出插件（原行为）
- **未保存误判修复**：主界面或保存成功后按 Esc 不再误弹"未保存的修改"。旧实现把富文本 innerHTML 直接与 Markdown 源码比较（永远不等），且保存后监听器未注销、残留在主界面。现改为：基线在首次渲染后用与保存完全一致的规范化管线取值；只比较内容/类型/标签/备注；保存成功后标记 saved 不再判脏
- **标签键盘选择"双蓝焦点"**：键盘 ↑/↓ 导航时压制鼠标 hover 高亮（kbd-nav 模式），鼠标移上又切回 hover 模式，消除两个蓝色选中的混乱

### 其他
- logo.png 缩至 128x128（母版 scripts/logo-master-256.png），消除打包时"logo 图片尺寸建议不超过 256x256 像素"提示；make-logo.py 改为纯 Python（标准库 zlib/struct），不再依赖 PIL

## 0.4.7 (2026-09-04) — bugfix

### 修复
- **Escape 键全面修复**：
  - 弹窗（设置/标签管理/更多标签/确认框）：Escape 关闭弹窗，不退出插件
  - 保存视图：Escape 检测是否有未保存修改，有则弹确认框，无则回退主面板
  - 主面板搜索框：Escape 清空搜索词（已有）
  - 标签输入建议列表：Escape 关闭列表（已有）
  - 全局 capture 阶段拦截：有 overlay 或下拉列表时阻止 Escape 冒泡到 uTools

## 0.4.6 (2026-09-04) — bugfix

### 修复
- **标签下拉列表自适应方向**：下方空间不足时自动向上展开，底部不再被窗口边缘裁剪

## 0.4.5 (2026-09-04) — bugfix

### 修复
- **标签建议列表被裁剪**：从 position: absolute 改为 position: fixed + z-index: 9999，不再被 body overflow: hidden 裁剪，下拉列表完整显示
- 键盘导航不循环（同 0.4.4）
- 剪贴板默认文本类型（同 0.4.4）

## 0.4.4 (2026-09-04) — bugfix

### 修复
- **Markdown 误判彻底解决**：剪贴板保存不再自动判定类型，统一默认为「文本」。用户需要保存 Markdown 请使用「闪藏：收藏 Markdown」命令
- **标签建议列表键盘导航**：到达底部/顶部后不再循环回到另一端
- 标签输入框 focus/blur 交互优化（同 0.4.3）

## 0.4.3 (2026-09-04) — bugfix

### 修复
- **Markdown 判定彻底修复（第三次）**：heredoc/模板字面量的正则转义在构建时出错，导致 regex 变成匹配字面量 `\\n` 而非换行符。改用 Node 脚本精确写入，确保正则正确
- **标签输入框**：加 focus 事件（点击/双击即可弹出建议列表）；pickTag 时抑制 blur 竞争，选中标签后列表正确关闭且芯片正确显示
- **标签建议列表**：不再截断为 6 个，全部展示（由 max-height: 200px 容器滚动）

## 0.4.2 (2026-09-04) — bugfix

### 修复
- **Markdown 判定彻底修复**：去掉 `(?<!\S)` lookbehind（Node 16 不支持导致正则崩溃），改用安全的块级行首检测
- **标签建议列表滚动**：给下拉列表加内联 `max-height: 200px + overflow-y: auto` 强制生效，不再撑开整个表单
- **已选标签区**也加了高度限制 + 滚动

## 0.4.1 (2026-09-04) — bugfix

### 修复
- Markdown 判定：收紧分隔线（---）检测，加了 `(?<!\S)` 前缀，普通文本中间出现 `---`（如「苹果---橙子」）不再误判为 markdown
- 保存视图标签区：已选标签和建议列表加了 max-height + 滚动条，多标签不再撑开整个表单
- 轻量提示（toast）：延迟退出窗口 1.4 秒，确保 toast 有时间显示
- 设置弹窗：去掉「当前方式」误导性文案

## 0.4.0 (2026-09-04)

### Markdown 判定
- 去掉行内标记（加粗、反引号、链接等）的自动检测——只保留块级特征（标题/列表/引用/围栏/分隔线），大幅减少误判

### 标签管理（全面改版）
- 新建标签输入框置顶（虚线边框区分），搜索框紧随其后
- 新增多选批量删除：勾选标签后批量删除按钮出现，一次移除多个标签
- 标签排序改为最近使用优先（复制/编辑过的标签自动排到前面）

### 富文本编辑
- 内联代码按回车跳出代码到新段落（不再卡在代码里）
- 代码块按钮：选中文本后生成代码块替换原段落（不再追加到后面）

### 成功提示（三选一设置）
- 三种成功提示风格：轻量提示（toast 气泡，默认）/ 完整提示（浮层）/ 不提示
- 主面板设置可视化三选一 + 浮层停留时长控制

## 0.3.0 (2026-09-04)

### 交互与体验
- 主面板进入时默认空搜索、显示全部收藏（不再把 uTools 同步的「闪藏」等词预填成搜索条件）
- 主面板新增「＋ 新增」入口，可手动新建收藏（默认文本，可在视图内切换类型）
- 标签输入框：**空白回车展示全部已存在标签**，建议列表限高 220px 内部滚动（不再撑高页面）
- 标签建议列表保持键盘 ↑/↓ + 回车 + Esc；标签管理弹窗新增**搜索过滤**
- 主面板标签多时：仅直显前 6 个未选标签，其余折叠进「**更多标签**」多选筛选面板（筛选改为多选、任一命中）

### 卡片布局
- 取消自生成的标题行；**备注更明显**（accent 强调色块 + 📝 前缀）
- 压缩卡片行高/字号，一屏展示更多条；图片收藏显示小缩略图

### 富文本编辑（Markdown）
- 内联代码「⟨码⟩」：选择文本后包裹为真正的行内代码（富文本可见样式，源码转反引号），不再插入字面符号
- 代码块内回车：一律手动插入换行，**不再被浏览器拆成新的代码块**；空行回车退出代码块
- 引用空行回车退出（沿用 0.2.0 语义，本次针对真实浏览器行为复核）

### 收藏行为设置（主面板「⚙ 设置」）
- 新增成功提示开关：**默认关闭**——保存后不显示「已收入」浮层，直接退出 uTools 回到之前的工作窗口
- 开启时可设定提示停留时长（0.6s / 1.2s / 2s / 3s）

## 0.2.0 (2026-09-04)

### 修订 1 —— Markdown 自动判定收紧
- 去掉「多行即 Markdown」的默认判定：现在多行文本也必须出现实际的 Markdown 特征（标题 / 列表 / 引用 / 代码围栏 / 分隔线 / 行内标记）才判定为 Markdown
- 多行纯段落（无任何 md 特征）判为普通文本

### 修订 2 —— 富文本编辑器（Markdown 编辑）
- 重写回车键语义，解决「卡在代码块 / 引用里出不来」的问题：
  - 代码块 & 引用：非空行回车 → 块内继续换行；空行回车 → 退出块，光标落到块后的新段落
  - 标题（H1~H6）：回车 → 新起普通段落，不再续标题
  - 退出时自动清理块内尾部空段落，避免保存出空引用行之类的脏内容
- 修复「⧉ 代码块」按钮无选区时的缺陷：不再插入看不见的字面符号，改为生成一个真正可输入的代码块

### 修订 3 —— 键盘选择（不再依赖鼠标）
- 标签建议列表：收藏/编辑视图输入标签出现建议时，支持 ↑/↓ 选择 + 回车添加 + Esc 关闭（悬停联动高亮）
- 主面板搜索结果：uTools 主搜索条不透传键盘事件（官方 API 限制），改为主面板内置搜索输入框；支持 ↑/↓ 选择收藏卡片 + 回车复制 + Esc 清空；进入主面板自动聚焦，从 uTools 搜索框带词进入时自动预填

### 工程
- npm run pack 新增 plugin.json 与 package.json 版本一致性校验

## 0.1.0 (初始版本)

- 快速收藏文本 / Markdown / 图片（支持全局快捷键保存剪贴板）
- 标签系统（自带标签管理：新建/重命名/删除，自动联动收藏）、备注
- 全文搜索（内容 / 标题 / 备注 / 标签，多关键词 AND，标签权重最高）
- Markdown 富文本 / 源码 / 预览三种编辑模式
- 点击卡片一键复制（图片复制原图）
- 深浅色自适应

# WORKMETHOD — 协作方法论（跨项目、跨领域）

> 本文件回答「**与人协作、把事办好、维护知识体系**的一般方法」，**不含项目细节，也不限于编码项目**——写作、调研、方案设计、项目治理同样适用。可整体复制到任何项目。
> 领域与项目的具体实践（如软件测试纪律、编码约束）属于**项目侧**，在各项目自己的契约/档案文档中体现，本文件不承载。
> 分层落地示例：项目契约与知识地图在项目根目录入口文件（本项目为 `AGENTS.md`）。

## 一、协作原则

### 1. 用户反馈表达的是「意图」，不是精确规格
协作者用日常语言描述现象与期望，且常自认"不专业"。实施时**不要逐字照搬**：还原真实问题，按有经验者的视角给出更合理的方案；必要时**主动纠正其字面要求**，并在交付说明中讲清「为什么这样改、代价是什么」。

### 2. 动手前先评估结构性代价
一个看似小的改动可能破坏其它机制。先问：这个改动动了谁的假设？有没有现成的机制/约定/既有成果会被牵连？改动的结构性代价与它的表面大小无关。

### 3. 有争议时给选项，而不是沉默照做
若存在多种合理取舍且影响结果，先说明取舍再选一个（或问对方），不要默默实现字面要求。

### 4. 诚实报告
验证是否真的覆盖了问题、有没有未验证的环节、哪些结论是推测，必须如实说明，不粉饰。

### 5. 对方说"和某样东西有关系"时，那是有价值的线索
说明现象依赖于一个被忽略的变量，顺着它查往往直达根因。

## 二、排障纪律（适用于任何问题的定位）

### 1. 真因纪律：同一现象修 2 次仍未解决，必须停下
停止在当前方向试错，构造**可量化的最小复现场景**、重新假设根因。在同一层面反复打转、而真因其实在另一层（现象层/机制层/事件与状态层），是最常见的浪费模式——再试之前先问"这是不是又是同一层的假设"。

### 2. 语义状态，不用瞬态近似
凡是表达「某人/某系统**当前处于某种模式/方式**」的判定，必须是**显式语义状态**（一个真实存在、由语义动作进入和退出的状态），而**不是用某个瞬态信号近似表达**——瞬态信号（"刚发生过什么"的痕迹，如时间上的临近、某个单次事件的残留）会随情境失效：快了误触发、慢了保护丢失。
- 通用判据：**这个量在语义上就该长期为真，还是只是"刚刚发生过"的痕迹？** 后者不能当"正处在"来用。
- 推论 1：进入与退出都应绑定**语义上明确的动作**（对方真实表达了意图），而非环境被动变化。
- 推论 2：同一状态若在两处表示（表征 + 判据；如展示与逻辑），必须走唯一入口同步，禁止直接改表现层。

### 3. 因果链必须真实走一遍
机制类问题，验证必须**真正驱动完整因果链**（每一步事件/步骤都真实发生），而不是验证端点假设——链条中间任何一步没真实发生过，结论即不成立。

## 三、验证纪律：「确认成立」不等于「证明锁住了」

**通用原则：验证与被验证对象不能共享同一个假设。** 若二者用了同一个前提（同一个常量、同一个模型、同一条未发生的路径），验证会给出假通过。

四条硬要求：
1. **双向验证**：要证明一条措施「能拦住错误」，必须先让错误发生（还原错误做法），确认措施报警；再恢复正确做法确认通过。只在正确状态看到"通过"不算数。
2. **条件与真实场景同量级**：验证条件的时序、规模、数值要模拟真实使用场景，不能比问题发生条件"更快更理想"（那样永远复现不了）。
3. **注入与断言相互独立**：不得为了避免某种情况而注入会掩盖该情况本身的值。
4. **路径覆盖证明**：先证明"这条路径真的走过了"，再谈结果对错。

## 四、知识体系（这套方法论的载体）

### 1. 分层渐进披露（核心思想）
注意力/上下文是稀缺资源：**常驻的越小越好，细节按需获取，历史归档冷存**。任何跨会话、跨协作者的知识体系都适用四层模型：

| 层 | 定位 | 进入时机 |
|---|---|---|
| L0 常驻契约 | 入口 + 硬规则 + 知识地图 | 每次自动可见，**保持极小** |
| L1 交接情报 | 当前状态、资源索引、指针 | 新协作者第一件事主动读 |
| L2 按需档案 | 模块/领域级的陷阱与须知 | 触及对应模块前才读 |
| L3 冷档 | 逐次明细、排查全史 | 查历史时才读 |
| 跨项目 | 本文件（方法论） | 被各项目入口按需指到 |

### 2. 内容单点存放（写入规则）
- 每个知识点**只在一个文件里完整写**，其他位置最多一句话 + 指针。
- 判定口诀：**会变、要查的 → 交接情报；"永远不许违反"的 → 契约；已做完的 → 更新日志；领域细节 → 档案；思想方法 → 本文件。**

### 3. 关键陷阱条目的写法与入库判定
- 写法三段式：**现象 / 真因 / 禁令**。禁令必须写明代价（"为什么不能"），否则后来者不知道代价就会"优化"回去。
- 入库判定（满足任一即写）：① 踩一次浪费超过一次往返；② 静默失败（不报错但结果错）；③ 反直觉的框架/环境约束；④ 同一问题修 ≥2 次才找到真因（必须记录"真因 vs 错误方向"）；⑤ 对方明确说"别再改回去"。
- 分层去向：无条件约束 → 契约层一句式规则 + 对应档案写来龙去脉；局部约定 → 只写档案；过程记录 → 冷档。

### 4. 待办独立（做完即删）
待办放独立位置，**完成即删**，不留"已完成"清单（完成的内容进更新日志）；若结论有长期价值，先迁移到档案/冷档再删。待办与交接情报混写会导致"不敢删、不维护"。

### 5. 阶段驱动的定期压缩（重要）
知识体系会自然膨胀。**每完成一批阶段成果（如若干版本/里程碑），或一次大坑收敛后**，应主动发起一轮体系维护：
1. **去重**：同一知识点是否在两处各写了一遍？→ 保留唯一出处，另一处改指针；
2. **校验指针**：入口地图里引用的文件/章节是否仍存在、仍准确；
3. **上浮沉淀**：反复出现的教训 → 去掉项目细节后提炼进本文件。**路由规则**：教训回答"怎么与人协作"→ §一；回答"问题该往哪查"→ §二；回答"怎么证明做对了"→ §三；回答"知识放哪/何时删"→ §四。只收**原则 + 至多一行匿名示例**，且须已跨项目复现（只出现过一次的先留项目侧，第二次出现再上浮）；放不进现有章节的新类别，先留项目侧，成组出现再增设条目（不轻率新开章）；故事化的过程记录归冷档，不进本文件；
4. **归档冷数据**：明细档超过千行级时按阶段分卷；
5. **瘦身交接情报**：已完成的待办、已并入主题归集的历史描述，从交接文件里删掉；
6. 维护本身记一条更新日志，并在交接文件里写明"下次维护应触发于哪"。

# AGENTS — Agent 协作指南（契约 · 知识地图 · 通用模板）

> 本文件与 `@WORKMETHOD.md` 组成**所有项目通用的模板核心**（编码或非编码项目均可复制使用）。
> 本文件**零项目内容**：项目背景、进展、架构、核心要素、项目硬规则、陷阱档案索引，全部由项目实例的交接文件（惯例名 `HANDOFF.md`）承载。
> 本文件每个会话自动注入上下文，**保持精简是体系的第一原则**——所有内容以指针 + 骨架为主，细节一律按需读取。

## 1. 开始工作（顺序固定）
1. 读 `@HANDOFF.md` —— 了解**项目**：背景、进展、架构、核心要素、项目硬规则、档案索引；
2. 读 `@WORKMETHOD.md` —— 了解**做事的方法**：协作原则、排障纪律、验证纪律（新项目或本文件变更后的首任务先通读，之后按需回查）；
3. 按本次任务触及的模块/领域，读 `docs/traps/` 对应档案（**动手前必读**，索引在 HANDOFF）；
4. 看 `@docs/TODO.md` 有无待办与本次任务的关系。

## 2. 怎么做事
- **一切做事原则、排障与验证纪律以 `@WORKMETHOD.md` 为准**（初读顺序见 §1），本文件不复制、不缩写。
- 若方法论已通读过，工作中牢记其结论；被触发回查时再取细节。
- 需要遵循的**项目侧硬规则与门禁**（如验证命令、交付方式）见 `HANDOFF.md` 对应章节。

## 3. 知识地图（约定骨架；项目按此填充，不得另立结构）

| 文档 | 约定承载 | 由谁填充 |
|---|---|---|
| `AGENTS.md`（本文件） | 通用契约、知识地图骨架、开始工作的路线 | 模板（跨项目共享；改 fundamentals 只改这里） |
| `WORKMETHOD.md` | 跨项目跨领域方法论：协作/排障/验证/知识体系 | 模板（**只经其 §四.5 上浮沉淀迭代**） |
| `HANDOFF.md` | **项目实例的全部情报**：背景、进展、架构、核心要素、项目硬规则、档案索引、跨模块决策 | 项目（随工作持续迭代） |
| `docs/TODO.md` | 项目待办（做完即删；结论先迁移再删） | 项目（随工作迭代） |
| `docs/traps/*.md` | 模块/领域级陷阱与须知（现象/真因/禁令三段式） | 项目（工作中不断充实） |
| `docs/CHANGELOG.md` | 阶段成果主题归集（同题修订合并） | 项目（每次任务收尾追加） |
| `docs/CHANGELOG-DETAIL.md` | 逐项明细归档（真因/过程全史，冷存；超千行按阶段分卷） | 项目 |
| `README.md` | 使用者/读者视角文档 | 项目 |
| 工具入口（如 `CLAUDE.md`、`.agents/agent.md`） | 各工具入口路径约定，**薄引用，不承载内容** | 工具形态决定 |

> 单点存放自检：**同一知识点不该在两份文档里各写一遍**；第二处出现改为一句话 + 指针。
> 迭代边界：**项目层知识**（HANDOFF/docs）随工作自然迭代；**方法论**（WORKMETHOD）只经上浮沉淀迭代——教训须去掉项目细节且跨项目复现才写入（WORKMETHOD §四.5 第 3 条）。

## 4. 收尾
每次任务收尾同步维护文档：TODO 删/记 → 成果进 CHANGELOG → HANDOFF 现状与档案索引 → 有新坑写 traps 三段式（判定标准与写法：WORKMETHOD §四.2/§四.3）→ 交验收方确认，不带走失败的验证结果交付。
